= useFileValidation

Validates upload form data against SHACL shapes from `tbox.ttl`.
Loads the TBox once on mount, then revalidates whenever the file, title, description, or WebID changes.

`validation.valid` is `false` if any required property is missing.
The `violations` array includes a property label and description so the UI can show actionable error messages.

== Returns

`validation`:: `{ valid, violations }`, result of the current SHACL check
`tboxError`:: Error message if the TBox could not be loaded, otherwise `null`
`isReady`:: `true` when the TBox is loaded and validation has run at least once

== See also

* link:../../../../infrastructure/validation/tboxValidator/README.adoc[tboxValidator] is the SHACL engine this hook calls to produce `validation.violations`.
* link:../../../../infrastructure/validation/tboxTypes/README.adoc[tboxTypes] defines the `PropertyViolation` and `ValidationResult` types returned by this hook.
* link:../useFileTypes/README.adoc[useFileTypes] loads the full list of type definitions from the same TBox, useful when building a type selector.
* link:../../components/FileUpload/README.adoc[FileUpload] is the component that calls this hook and renders the violation messages.

== Navigation

.Parent
link:../README.adoc[file-explorer/hooks]

.Uses
* link:../../../../infrastructure/validation/fileTypeRegistry/README.adoc[fileTypeRegistry]
* link:../../../../infrastructure/validation/tboxValidator/README.adoc[tboxValidator]
