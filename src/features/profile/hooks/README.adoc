= profile/hooks

React hooks for profile and social data.

== Contents

link:useProfile/README.adoc[useProfile]:: Read/write the logged-in user's `foaf:name` and `foaf:img` fields
link:useContacts/README.adoc[useContacts]:: Read and modify the `foaf:knows` contact list
link:useAccessRequests/README.adoc[useAccessRequests]:: Load, approve, and deny access requests from the user's LDP inbox

== Navigation

.Parent
link:../README.adoc[profile]
