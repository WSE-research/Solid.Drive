= tboxValidator

Loads SHACL shapes from `tbox.ttl` and validates file metadata against them.
Validation returns `{ valid, violations, shape }`.
Each violation includes the property `path`, `label`, and `description` so the UI can show actionable error messages.

== Functions

[cols="1,3", options="header"]
|===
| Function | Description

| `loadTBox(tboxUri?, fetch?)` | Fetches, parses, and caches the TBox; returns `{ shapes, parents }`
| `validateMetadata(metadata, typeUri, shapes, parents)` | Finds the shape for the type, walks the inheritance hierarchy (BFS with cycle detection), and checks required properties
| `getShapeForType(typeUri, shapes, parents)` | Merges shape definitions from the type and all its ancestors
| `resetTBoxCache()` | Clears cached shapes (used in tests)
|===

== See also

* link:../tboxParser/README.adoc[tboxParser] parses the raw SHACL NodeShapes that `loadTBox` depends on.
* link:../tboxTypes/README.adoc[tboxTypes] defines the `ValidationResult` and `PropertyViolation` types that this module returns.
* link:../../../features/file-explorer/hooks/useFileValidation/README.adoc[useFileValidation] is the React hook that loads the TBox and drives this validator.

== Navigation

.Parent
link:../README.adoc[validation]

.Uses
* link:../../../config/README.adoc[config]
* link:../tboxParser/README.adoc[tboxParser]
* link:../tboxTypes/README.adoc[tboxTypes]
