= tboxParser

Parses SHACL NodeShape definitions from Turtle text with no I/O side effects.

== Returns

[cols="1,3", options="header"]
|===
| Value | Description

| `shapes` | Map of shape URI -> `ShapeDefinition` for all `sh:NodeShape` nodes
| `parents` | Map of child URI -> parent URIs via `rdfs:subClassOf`
|===

== See also

* link:../tboxValidator/[tboxValidator] consumes the `shapes` and `parents` maps that this module produces when validating file metadata.
* link:../tboxTypes/[tboxTypes] defines `ShapeDefinition` and `PropertyConstraint`, the types that the parsed output conforms to.
* link:../fileTypeRegistry/[fileTypeRegistry] calls this parser when loading file type definitions from the TBox.

== Navigation

.Parent
link:../[validation]

.Uses
* link:../../../config/[config]
* link:../tboxTypes/[tboxTypes]
