= useContactRejections

Fetches rejection notifications from a user's inbox and exposes a handler to dismiss individual entries.
Used by both `SharedWithMeSection` (file-explorer) and `ContactsList` (profile).

== Returns

`fileRejections`:: Map of container URI -> `AccessRejection` for all active rejections
`handleClearRejection(containerUri)`:: Removes the rejection for the given URI from the local map

== See also

* link:../../../infrastructure/inbox/inboxAccess/[inboxAccess] reads the inbox to find and parse rejection notification messages.
* link:../../../features/file-explorer/components/SharedWithMeSection/[SharedWithMeSection] receives the rejection state from this hook for the file-explorer shared-with-me view.
* link:../../../features/profile/components/ContactsList/[ContactsList] receives the rejection state from this hook for the contacts list view.

== Navigation

.Parent
link:../[shared/hooks]

.Uses
* link:../../../infrastructure/inbox/inboxAccess/[inboxAccess]
