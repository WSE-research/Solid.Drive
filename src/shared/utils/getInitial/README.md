# getInitial

## Overview

`getInitial(name: string): string`

Returns the first character of a display name, uppercased. Used as a fallback in the `Avatar` component when no profile image is available. Returns `"?"` for empty strings.
