= env

Typed access to Vite environment variables.
Exports a single `ENV` object with `mode`, `dev`, `prod`, and `baseUrl` fields.

== Usage

Components and services should import from here rather than reading `import.meta.env` directly, keeping env access consistent and testable.

== Navigation

.Parent
link:../[config]
